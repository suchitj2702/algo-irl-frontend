# Example environment configuration for AlgoIRL Frontend
# Copy this file to .env and update with your values

# Development API URL (used when running npm run dev)
# Leave empty to use relative URLs with Vite proxy
VITE_API_URL=

# Production API URL (used when building for production)
# This will override the default hardcoded URL
VITE_PRODUCTION_API_URL=https://your-api-domain.com

# Alternative: Use same URL for both dev and production
# VITE_API_URL=https://your-api-domain.com 